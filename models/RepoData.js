class RepoData {
    constructor(id, title,language, repoUrl, description,) {
      this.id = id;
      this.title = title;
      this.language = language;
      this.repoUrl = repoUrl;
      this.description = description;
    }
  }

  module.exports = RepoData